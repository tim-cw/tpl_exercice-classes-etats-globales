/** Componsante ColorTheme de TimTools */
export default class ColorTheme {
  /**
   * Méthode constructeur
   * @param {HTMLElement} element - Élément HTML sur lequel la composante est instanciée
   */
  constructor(element) {
    this.init();
  }

  /**
   * Méthode d'initialisation
   */
  init() {}
}
